(() => {
    // var nombre = "pablo" //!deprecado evitar usar
    const nombre: string = "pablo"; //? instead of var use it, 'const'.
    let age: number = 40; //? use 'let' in case its value can change, avoid 'var' use.
})()